.container
  .masthead
    %a.title{:href=>"/"}
      %div
        %p <b>Khan</b>Academy Translations
    %nav
      %ul
        %li.welcome
          Welcome, <span class="welcome-name">#{current_user.name}</span>!
        %li.logout
          =link_to("Logout", "/logout", :method => :delete) #, :confirm => "Are you sure?")
        - if current_user.admin?
          %li{ :class => 'pull-right'}
            = link_to 'All users', users_index_path
