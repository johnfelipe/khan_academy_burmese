-#view for translations

%html
  %head
    = stylesheet_link_tag 'videos'
  %body
    = render :partial => "shared/dashhead"
    = render :partial => "shared/nav"

    .container
      .content
        %h4{:class => "list-title"} Videos I Have Translated
        %table.table.table-bordered{:id => "list_table"}
          %tr
            %th.kabt_course Course
            %th.kabt_title Title
            %th.kabt_dd Due Date


          - @comp_trans.each do |video|
            %tr 
              %td= video.course
              %td= video.title
              %td= video.due_date.strftime('%b %d %Y')

        %h4{:class => "list-title"} Videos I Have Digitized
        %table.table.table-bordered{:id => "list_table"}
          %tr
            %th.kabt_course Course
            %th.kabt_title Title
            %th.kabt_dd Due Date

          - @comp_digi.each do |video|
            %tr 
              %td= video.course
              %td= video.title
              %td= video.due_date.strftime('%b %d %Y')

        %h4{:class => "list-title"} Videos I Have QA'd
        %table.table.table-bordered{:id => "list_table"}
          %tr
            %th.kabt_course Course
            %th.kabt_title Title
            %th.kabt_dd Due Date

          - @comp_qa.each do |video|
            %tr 
              %td= video.course
              %td= video.title
              %td= video.due_date.strftime('%b %d %Y')